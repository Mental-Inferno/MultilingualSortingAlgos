#include <stdio.h>

int main (void) {

    /*  Quick-Sort Algorithm for a 1D array of int values rom least to greatest
        -orgainzes values from least at the start of the array to greatest at the end
    
        Function prototype:
        (void)Qsort-1INT-LG(int* arrayPtr, int arraySize)
    */

   /*  Quick-Sort Algorithm for a 2D array of int values rom least to greatest
        -orgainzes values in an nxn array from the start of the array to greatest at the end
    
        Function prototype:
        (void)Qsort-2INT-LG(int* arrayPtr, int rowSize, int colSize)
    */


    return 0;
}